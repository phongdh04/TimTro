package com.timtro.core.config;

import com.timtro.core.common.BaseEntity;
import org.springframework.data.annotation.CreatedBy;
import org.springframework.data.annotation.LastModifiedBy;

// Add imports for SecurityContextHolder checking

/**
 * Update BaseEntity to include auditing for user who made changes.
 */
// This logic is actually placed in BaseEntity, but we need the AuditorAware bean.
